<odoo>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="its_vendor_field.list">
      <field name="name">its_vendor_field list</field>
      <field name="model">its_vendor_field.its_vendor_field</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="its_vendor_field.action_window">
      <field name="name">its_vendor_field window</field>
      <field name="res_model">its_vendor_field.its_vendor_field</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="its_vendor_field.action_server">
      <field name="name">its_vendor_field server</field>
      <field name="model_id" ref="model_its_vendor_field_its_vendor_field"/>
      <field name="state">code</field>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="its_vendor_field" id="its_vendor_field.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="its_vendor_field.menu_1" parent="its_vendor_field.menu_root"/>
    <menuitem name="Menu 2" id="its_vendor_field.menu_2" parent="its_vendor_field.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="its_vendor_field.menu_1_list" parent="its_vendor_field.menu_1"
              action="its_vendor_field.action_window"/>
    <menuitem name="Server to list" id="its_vendor_field" parent="its_vendor_field.menu_2"
              action="its_vendor_field.action_server"/>
    -->
  </data>
</odoo>