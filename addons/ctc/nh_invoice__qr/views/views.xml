<odoo>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="nh_invoice__qr.list">
      <field name="name">nh_invoice_QR list</field>
      <field name="model">nh_invoice__qr.nh_invoice__qr</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="nh_invoice__qr.action_window">
      <field name="name">nh_invoice_QR window</field>
      <field name="res_model">nh_invoice__qr.nh_invoice__qr</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="nh_invoice__qr.action_server">
      <field name="name">nh_invoice_QR server</field>
      <field name="model_id" ref="model_nh_invoice__qr_nh_invoice__qr"/>
      <field name="state">code</field>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="nh_invoice_QR" id="nh_invoice__qr.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="nh_invoice__qr.menu_1" parent="nh_invoice__qr.menu_root"/>
    <menuitem name="Menu 2" id="nh_invoice__qr.menu_2" parent="nh_invoice__qr.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="nh_invoice__qr.menu_1_list" parent="nh_invoice__qr.menu_1"
              action="nh_invoice__qr.action_window"/>
    <menuitem name="Server to list" id="nh_invoice__qr" parent="nh_invoice__qr.menu_2"
              action="nh_invoice__qr.action_server"/>
    -->
  </data>
</odoo>